services:
  db:
    image: postgres:18
    container_name: amae_db
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PGDATA=/var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"
    volumes:
      - amae_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-amae} -d ${POSTGRES_DB:-amae}"]
      interval: 3s
      timeout: 5s
      retries: 10
      start_period: 10s

  redis:
    image: redis:7-alpine
    container_name: amae_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - amae_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 5s
      retries: 10
      start_period: 5s

  web:
    build: .
    container_name: amae_web
    restart: unless-stopped
    command: bash -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - POSTGRES_HOST=db
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  tailwind:
    image: node:24-slim
    container_name: amae_tailwind
    user: "node"
    working_dir: /app
    tty: true
    volumes:
      - .:/app
    command: sh -c "npm install && npm run dev:css"
    depends_on:
      - web

volumes:
  amae_postgres_data:
  amae_redis_data:

