[pytest]
DJANGO_SETTINGS_MODULE = amae.settings
python_files = test*.py
addopts = --strict-markers -n auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::ResourceWarning
    ignore::PendingDeprecationWarning
