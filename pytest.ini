[pytest]
python_files = test*.py
DJANGO_SETTINGS_MODULE = marathon.settings
addopts = --strict-markers --no-migrations --ignore=landingpages/tests/ --reuse-db -n auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::ResourceWarning
    ignore::PendingDeprecationWarning